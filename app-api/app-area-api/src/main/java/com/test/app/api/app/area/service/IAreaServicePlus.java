package com.test.app.api.app.area.service;

import com.test.app.api.app.area.model.Area;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.ResponseBody;

/**
 * 接收json类型，直接以对象作为参数
 */
public interface IAreaServicePlus {
    /**
     * 插入区域
     * @param model
     * @return
     */
//    @RequestMapping(path = "/insertplus",method = RequestMethod.POST)
//    @ResponseBody
//    int insertAreaPlus(Area model);

    /**
     * 更新区域信息
     * @param model
     * @return
     */
//    @RequestMapping(path = "/updateplus",method = RequestMethod.POST)
//    @ResponseBody
//    int updateAreaPlus(Area model);
}
